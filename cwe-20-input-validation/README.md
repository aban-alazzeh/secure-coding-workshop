## Setup

From the `cwe-20-input-validation` directory:

bash

python -m venv .venv
source .venv/bin/activate        # Linux/macOS

# OR
.\.venv\Scripts\Activate.ps1     # Windows

pip install -r requirements.txt
